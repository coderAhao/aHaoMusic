<!--pages/video/video.wxml-->
<wxs src="../../utils/format.wxs" module="format" />
<view class="video">
  <view class="item" wx:for="{{topMVs}}" wx:key="id">
    <video-item item="{{item}}" class="item" bindtap="handleVideoItemClick" data-item="{{item}}" />
  </view>
</view>